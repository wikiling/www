module Main where

import Lib ()

-- fragments
-- Deal 2019

-- phenomena
-- downward monotone quantifiers
-- focus
-- coordination
--      & non-canonical
-- oblique arguments in scope-freezing constructions

head' :: [a] -> a
head' [] = error "Can't call head on an empty list, dummy!"
head' (x : _) = x

main :: IO ()
main = return ()